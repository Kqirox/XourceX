# Public runtime configuration for the XourceX frontend.
# Copy to .env.local and adjust per environment. Never commit secrets.

# Base URL of the backend API (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:8080

# Federated / cross-chain bridge SDK key (optional).
# NEXT_PUBLIC_BRIDGE_API_KEY=